# Data artifacts

Future cases may place machine-readable benchmark measurements or generated curve
tables here. The first case keeps its full curve data embedded in the standalone
HTML report so it remains portable.
