# Local-only external code

Place disposable or machine-specific third-party checkouts here. Everything except
this README is ignored. Reproducibility dependencies belong in `../tracked/` as
pinned Git submodules.
